name: Publish JSON Schema (Legacy)

# NOTICE: Schema export and deployment has been integrated into the main build workflow
# This workflow is kept for backward compatibility but does minimal work
# See: .github/workflows/build-publish_to_pypi.yml (export_schema and deploy_schema jobs)

on:
  workflow_dispatch:
    inputs:
      message:
        description: 'This workflow has been integrated into build-publish_to_pypi.yml'
        required: false
        default: 'Schema export is now part of the main build workflow'

jobs:
  redirect_notice:
    name: Redirect Notice
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📢 NOTICE: Schema Export Has Been Integrated"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "Schema export and deployment is now handled by the main build workflow:"
          echo "  → .github/workflows/build-publish_to_pypi.yml"
          echo ""
          echo "The schema is automatically:"
          echo "  ✓ Exported after successful cp313-manylinux wheel build"
          echo "  ✓ Deployed to GitHub Pages for:"
          echo "    - PR previews (with warning banner)"
          echo "    - Master branch pushes"
          echo "    - Release tags"
          echo "    - Nightly builds"
          echo ""
          echo "View the main workflow run for schema export details."
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"