# SUEWS-agent Logo

Use `suews-agent-logo.svg` as the canonical SUEWS-agent icon asset.

This mark is based on the current SUEWS logo and adds a subtle circuit-trace layer to suggest the agent / workflow layer. Do not use generated wordmark images for `SUEWS-agent`; compose any text label programmatically in the consuming site or README so spacing and punctuation stay controllable.

If future SUEWS-agent icons or favicons are needed, derive them from this SVG master.
